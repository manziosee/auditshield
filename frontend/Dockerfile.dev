FROM node:22-alpine

WORKDIR /app

# Install deps first (layer cache)
COPY package*.json ./
RUN npm ci

COPY . .

ENV HOST=0.0.0.0
EXPOSE 3000

CMD ["npm", "run", "start", "--", "--host", "0.0.0.0", "--port", "3000", "--poll", "2000"]
